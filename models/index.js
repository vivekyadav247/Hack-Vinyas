// Central export file for all models
const Team = require('./Team');
const Admin = require('./Admin');
const OTP = require('./OTP');
const PPTSubmission = require('./PPTSubmission');

module.exports = {
    Team,
    Admin,
    OTP,
    PPTSubmission
};
